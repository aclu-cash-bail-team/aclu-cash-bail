{
  "name": "aclu-cash-bail",
  "version": "1.0.0",
  "description": "This repository hosts the visualization code for the ACLU PA Cash Bail Report.",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf dist/ && parcel watch index.html",
    "build": "rm -rf dist/ && parcel build index.html --public-url='/aclu-cash-bail'",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "deploy-test": "git subtree push --prefix dist origin gh-pages-test",
    "lint": "./node_modules/.bin/eslint src/js/*.js src/js/classes/*.js",
    "fix": "./node_modules/.bin/eslint --fix src/js/*.js src/js/classes/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/apothuku/aclu-cash-bail.git"
  },
  "keywords": [],
  "author": "Alex Graves and Arjun Pothukuchi",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.16.0",
    "parcel-bundler": "^1.12.4"
  },
  "bugs": {
    "url": "https://github.com/apothuku/aclu-cash-bail/issues"
  },
  "homepage": "https://github.com/apothuku/aclu-cash-bail#readme",
  "dependencies": {
    "d3": "^6.2.0",
    "topojson": "^3.0.2"
  }
}
